(()=>{const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const count=$('livePlayerCount'),list=$('livePlayerList');if(!count||!list)return;try{const r=await fetch('/api/players',{credentials:'same-origin'});if(!r.ok)throw new Error('unavailable');const d=await r.json();const players=d.players||d.Players||[];count.textContent=String(d.count??d.Count??players.length);list.innerHTML=players.length?players.map(p=>`<div class="live-player"><span class="online-dot"></span><strong>${esc(p.name??p.Name)}</strong><small>${esc(p.location??p.Location??'Arkovia')}</small></div>`).join(''):'<div class="community-empty">No adventurers are online right now.</div>';}catch{count.textContent='—';list.innerHTML='<div class="community-empty">Live player status is temporarily unavailable.</div>';}}document.addEventListener('DOMContentLoaded',()=>{load();setInterval(load,30000);});})();
